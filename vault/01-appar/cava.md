# cava

Ljudvisualisering. Två configs:

- `config` — fristående/terminal-användning.
- `waybar.conf` — variant som körs inbäddad i [[waybar]] via `waybar/scripts/cava-waybar.sh`.

`shaders/` och `themes/` — extra visuella varianter.
