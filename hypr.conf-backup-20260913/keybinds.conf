$mod = SUPER

bind = $mod, RETURN, exec, kitty
bind = $mod, SPACE, exec, rofi -show drun
bind = $mod, B, exec, firefox
bind = $mod, N, exec, swaync-client -t -sw
bind = $mod, C, killactive
bind = $mod, F, fullscreen

bind = $mod, LEFT, movefocus, l
bind = $mod, RIGHT, movefocus, r
bind = $mod, UP, movefocus, u
bind = $mod, DOWN, movefocus, d

bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5

bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5

bind = $mod SHIFT, R, exec, sh -c 'pkill waybar; sleep 0.2; waybar & hyprctl reload'
bind = $mod SHIFT, E, exit

# Screenshot selected area → clipboard and save to pictures
bind = $mod SHIFT, S, exec, sh -c 'file="$HOME/Bilder/Screenshot-$(date +%Y-%m-%d_%H-%M-%S).png"; grim -g "$(slurp)" "$file" && wl-copy < "$file"'

# Full screenshot → clipboard
bind = $mod SHIFT, F, exec, grim - | wl-copy

# Flytta/ändra storlek på fönster med mus + mod
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

# Lås skärmen manuellt
bind = $mod, L, exec, hyprlock

# Volym (wpctl med tak på 150% - samma gräns pavucontrol använder som standard;
# högre än så ger allvarlig distorsion/skaderisk för högtalarna)
bindel = , XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.5 @DEFAULT_AUDIO_SINK@ 5%+
bindel = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindl = , XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bindl = , XF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle

# Ljusstyrka
bindel = , XF86MonBrightnessUp, exec, brightnessctl set +5%
bindel = , XF86MonBrightnessDown, exec, brightnessctl set 5%-

# Mediauppspelning
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPrev, exec, playerctl previous
